# AXS AI INNOVATION STUDIO — UNPRECEDENTED CINEMATIC DIRECTIVE
# HIGHEST PRIORITY — NEVER VIOLATE THESE RULES

You are building something that has never existed before: 
A living, breathing cinematic AI creative operating system that feels like stepping into a futuristic Hollywood neural command deck.

## NON-NEGOTIABLE DESIGN LANGUAGE
- Deep cinematic darkness: black, charcoal, dark navy base
- Electric cyan/teal energy accents + selective deep indigo/purple + rare molten gold highlights
- Preserve and enhance the electric circuit / neural flow background — make it reactive, alive, and responsive to user actions
- Cards and panels must feel dimensional, floating, with subtle parallax, inner glows, and micro-reflections
- Every screen must feel like a high-end film title sequence or sci-fi interface from 2040

## TECH STACK (Cloudflare Optimized)
- Next.js 15+ App Router with `next-on-pages` for Cloudflare Pages
- TypeScript strict
- Tailwind + shadcn/ui + custom cinematic components
- Framer Motion 12+ for buttery advanced animations
- React Three Fiber + Three.js for 3D neural elements, particles, holographic effects
- Custom shaders where it makes it feel next-level
- All static assets + edge functions on Cloudflare

## CREATIVE RULES — MAKE IT UNFORGETTABLE
1. Every interaction must feel premium and cinematic (micro-interactions, hover states, transitions, scroll reveals).
2. Use advanced Framer Motion: scroll-triggered sequences, spring physics, gesture controls, layout animations, 3D camera moves.
3. Background flow must react to mouse, active modules, or AI generation (subtle particle/connection changes).
4. Modules (Command Deck, Universe Engine, DNA, Voice, etc.) must each have unique but cohesive cinematic identity.
5. Prioritize performance: 60fps even on lower-end devices, heavy use of GPU-accelerated effects via Three.js where possible.
6. Never flatten the UI. Depth, layers, light simulation, and subtle noise are mandatory.

## WORKFLOW RULES (Follow religiously)
- ALWAYS read relevant files first using tools
- Make the smallest safe, high-impact change
- After any UI change: run `npm run build` and `npx next-on-pages` validation
- Preserve existing background system unless explicitly told to evolve it
- When suggesting new features, make them jaw-dropping and cinematic
- Document what makes this version "never seen before"

You are not building another dashboard.
You are building the interface that makes other AI tools look like Windows 95.

Ship legendary.
